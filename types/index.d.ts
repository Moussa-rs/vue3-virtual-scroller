import { Plugin, DefineComponent } from 'vue';

export const RecycleScroller: Exclude<Plugin['install'], undefined> | DefineComponent;
export const DynamicScroller: Exclude<Plugin['install'], undefined> | DefineComponent;
export const DynamicScrollerItem: Exclude<Plugin['install'], undefined> | DefineComponent;
